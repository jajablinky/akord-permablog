import { NodeService } from "./node";
import { reactionEmoji, objectType } from "../constants";
import { Memo } from "../types/node";
declare class MemoService extends NodeService<Memo> {
    static readonly reactionEmoji: typeof reactionEmoji;
    objectType: objectType;
    NodeType: typeof Memo;
    /**
    * @param  {string} vaultId
    * @param  {string} message
    * @returns Promise with new node id & corresponding transaction id
    */
    create(vaultId: string, message: string): Promise<{
        memoId: string;
        transactionId: string;
    }>;
    /**
    * @param  {string} memoId
    * @param  {reactionEmoji} reaction
    * @returns Promise with corresponding transaction id
    */
    addReaction(memoId: string, reaction: reactionEmoji): Promise<{
        transactionId: string;
    }>;
    /**
     * @param  {string} memoId
     * @param  {reactionEmoji} reaction
     * @returns Promise with corresponding transaction id
     */
    removeReaction(memoId: string, reaction: reactionEmoji): Promise<{
        transactionId: string;
    }>;
    private memoVersion;
    private memoReaction;
    private deleteReaction;
    private getReactionIndex;
}
export { MemoService };
