import { Encryptable, Keys } from "@akord/crypto";
import { ProfileDetails } from "./profile-details";
export declare type RoleType = "VIEWER" | "CONTRIBUTOR";
export declare type StatusType = "ACCEPTED" | "PENDING" | "REVOKED" | "INVITED";
export declare class Membership extends Encryptable {
    id: string;
    owner: string;
    createdAt: string;
    updatedAt: string;
    status: StatusType;
    address: string;
    role: RoleType;
    data?: string[];
    encPublicSigningKey: string;
    memberDetails: ProfileDetails;
    vaultId: string;
    keys: Keys[];
    constructor(membershipProto: any, keys?: Array<Keys>);
}
export declare type MembershipKeys = {
    isEncrypted: boolean;
    keys: Keys[];
    publicKey?: string;
};
