import { Akord } from "./akord";
import { AkordWallet } from "@akord/crypto";
import ApiAuthenticator from "./api/api-authenticator";
declare class Auth {
    apiAuthenticator: ApiAuthenticator;
    constructor();
    /**
    * @param  {string} email
    * @param  {string} password
    * @returns Promise with Akord Client instance, JWT token & Akord Wallet
    */
    signIn: (email: string, password: string) => Promise<{
        akord: Akord;
        wallet: AkordWallet;
        jwtToken: string;
    }>;
    /**
    * @param  {string} email
    * @param  {string} password
    * @param  {any} clientMetadata JSON client metadata, ex: { clientType: "CLI" }
    * @returns Promise with Akord Wallet
    */
    signUp: (email: string, password: string, clientMetadata?: any) => Promise<AkordWallet>;
    /**
    * @param  {string} email
    * @param  {string} code
    * @returns
    */
    verifyAccount: (email: string, code: string) => Promise<void>;
}
export { Auth };
