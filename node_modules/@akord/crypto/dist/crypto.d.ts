import { Encrypter } from "./encrypter";
import { EncryptionType, Keys } from "./encrypter/types";
import { Wallet } from "./wallet";
export declare enum WalletType {
    Akord = "Akord",
    Arweave = "Arweave"
}
export interface CryptoOptions {
    walletType?: WalletType;
    wallet?: Wallet;
    backupPhrase?: string;
    encBackupPhrase?: string;
    password?: string;
    encryptionType?: EncryptionType;
    keys?: [Keys];
}
export declare class CryptoClass {
    private _config;
    private _wallet;
    private _encrypter;
    constructor(config: CryptoOptions);
    configure(config: CryptoOptions): Promise<CryptoOptions>;
    wallet(): Wallet;
    encrypter(): Encrypter;
    encrypt(input: string): Promise<any>;
    decryptRaw(input: any): Promise<any>;
    decrypt(input: string): Promise<string>;
    private configureEncrypter;
    private configureWallet;
}
export declare const Crypto: CryptoClass;
